# Core numerical / imaging stack (required)
numpy>=1.24
opencv-python>=4.8
scipy>=1.11
scikit-image>=0.22
scikit-learn>=1.3
matplotlib>=3.7
plotly>=5.18
rich>=13.0
tqdm>=4.66

# Optional GPU acceleration. Install the CuPy wheel matching your CUDA toolkit,
# e.g. `pip install cupy-cuda12x`. When present (with a CUDA device) the engine
# runs the identical algorithms on the GPU; otherwise it falls back to NumPy.
# cupy-cuda12x>=13.0

# Optional JIT acceleration for CPU fallback kernels.
numba>=0.59

# Testing
pytest>=8.0
